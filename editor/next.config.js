module.exports = {
  experimental: {
    // externalDir: true,
  },
}

// const withTM = require('next-transpile-modules')(['ts-closure-library']) // pass the modules you would like to see transpiled

// module.exports = withTM({})
